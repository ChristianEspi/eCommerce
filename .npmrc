# El contrato de runtime vive en package.json (engines.node) y .nvmrc.
# Con engine-strict, `npm ci` FALLA en un Node fuera de contrato (p. ej. 20) en
# vez de construir con él en silencio: en desarrollo, en CI y en el build de Amplify.
engine-strict=true
